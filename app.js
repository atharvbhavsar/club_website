document.querySelector('.div2').addEventListener('click', function() {
    const membersDiv = document.querySelector('.members');
    if (membersDiv) {
      membersDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
    } else {
      console.error('No element with the class "members" found.');
    }
  });
  document.querySelector('.div1').addEventListener('click', function() {
    const membersDiv = document.querySelector('.banner-container');
    if (membersDiv) {
      membersDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
    } else {
      console.error('No element with the class "members" found.');
    }
  });
  document.querySelector('.div3').addEventListener('click', function() {
    const membersDiv = document.querySelector('.join-us');
    if (membersDiv) {
      membersDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
    } else {
      console.error('No element with the class "members" found.');
    }
  });
  document.querySelector('.div4').addEventListener('click', function() {
    const membersDiv = document.querySelector('.event-page');
    if (membersDiv) {
      membersDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
    } else {
      console.error('No element with the class "members" found.');
    }
  });
  document.querySelector('.div5').addEventListener('click', function() {
    const membersDiv = document.querySelector('.about-us');
    if (membersDiv) {
      membersDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
    } else {
      console.error('No element with the class "members" found.');
    }
  });
  document.querySelector('.div6').addEventListener('click', function() {
    const membersDiv = document.querySelector('.gallery');
    if (membersDiv) {
      membersDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
    } else {
      console.error('No element with the class "members" found.');
    }
  });
  document.querySelector('.div7').addEventListener('click', function() {
    const membersDiv = document.querySelector('.contact-us');
    if (membersDiv) {
      membersDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
    } else {
      console.error('No element with the class "members" found.');
    }
  });
  


  